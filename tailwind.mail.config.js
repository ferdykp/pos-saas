/** Email uses the same utility syntax, without browser resets or CSS variables. */
export default {
    content: ['./resources/views/vendor/notifications/email.blade.php'],
    corePlugins: { preflight: false },
};
